# Быстрый старт: Деплой и миграции

## 🚀 Применить миграцию на проде (прямо сейчас)

### 1. Настройте сервер в Makefile
```bash
# Отредактируйте Makefile, строка 5:
REMOTE_SERVER = root@poleeducation.in.ua
```

### 2. Примените миграцию
```bash
make migrate-prod
```

Готово! Миграция `sessionId` применена.

---

## 📦 Сборка фронтенда локально (решение проблемы с RAM)

### Почему это нужно?
На вашем сервере 2GB RAM не хватает для сборки React приложения.

### Решение: Собирайте локально!

```bash
# 1. Соберите фронтенд на своем компьютере
make build-frontend-local

# 2. Загрузите на сервер
make deploy-frontend-local
```

### Или всё сразу (backend + миграции + frontend):
```bash
make deploy-with-local-build
```

---

## 🔥 Переезд на Vite (рекомендуется!)

### Почему Vite?
- ⚡ Сборка в **10 раз быстрее** (30 сек вместо 5 мин)
- 💾 Требует **500MB RAM** вместо 2GB
- 🚀 Мгновенный hot reload
- ✅ Create React App **deprecated** (больше не поддерживается)

### Когда мигрировать?
Когда будет 1-2 часа свободного времени.

### Как?
Следуйте инструкции в [VITE_MIGRATION.md](VITE_MIGRATION.md)

---

## 📚 Документация

- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Полное руководство по деплою
- **[VITE_MIGRATION.md](VITE_MIGRATION.md)** - Миграция на Vite

---

## ✅ Чеклист для прямо сейчас

1. **Применить миграцию sessionId:**
   ```bash
   make migrate-prod
   ```

2. **Настроить локальную сборку** (опционально):
   - Отредактировать `REMOTE_SERVER` в Makefile
   - Протестировать: `make build-frontend-local`

3. **Запланировать миграцию на Vite** (в будущем):
   - Это сэкономит вам массу времени при разработке
   - Решит проблему с RAM на сервере навсегда

---

## 🆘 Если что-то пошло не так

### Откатить миграцию:
```bash
make migrate-undo-prod
```

### Восстановить базу из бэкапа:
```bash
make restore-db
```

### Проверить логи:
```bash
docker logs poleeducation_backend
```

---

## 💡 Рекомендации

1. **Всегда делайте бэкап перед миграцией:**
   ```bash
   make backup-db
   ```

2. **Используйте локальную сборку** для экономии RAM

3. **Мигрируйте на Vite** когда будет время - это изменит вашу жизнь к лучшему!

---

## 📞 Контакты

Вопросы? Проблемы?
Проверьте полную документацию в [DEPLOYMENT.md](DEPLOYMENT.md)
